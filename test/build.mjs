/*
The following import will ensure that all the exported files properly us .js when importing
This ensures that we will be able to use the util library without the --experimental-modules flag
*/
import * as Utils from '../build/index.js';
